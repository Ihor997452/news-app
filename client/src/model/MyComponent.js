export class MyComponent {
    constructor(text, multipartFile) {
        this.text = text
        this.cssStyle = {"overflow": "scroll", "position": "relative"}
        this.multipartFile = multipartFile
    }
}