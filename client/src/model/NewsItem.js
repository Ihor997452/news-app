export class NewsItem {
    constructor(id, title, caption, imagePath, components) {
        this.id = id;
        this.title = title;
        this.caption = caption;
        this.imagePath = imagePath;
        this.components = components;
    }
}